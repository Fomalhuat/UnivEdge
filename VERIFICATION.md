# VERIFICATION.md — UnivEdge 验证协议

> 版本：v0.3（待用户复核）｜ 日期：2026-08-13
> 领域内核文件之二：定义"如何验证 UnivEdge 的产物"，与 METHODOLOGY.md 配套。
> 依赖：METHODOLOGY.md（检查项的来源）、config/runtime-contract.md（执行环境要求）。
> v0.3 变更（源自 docs/07 复盘）：新增 NC-06 外部锚点对照、RC-04 交叉验证独立性、
> RC-05 测试设计审查、CC-01 约定一致性。

---

## 使用方式（加载协议）

本文**不是常驻文件**（不进 L1），只在执行验证时按需加载：

| 层级 | 内容 | 加载时机 |
|---|---|---|
| L1 常驻（在 METHODOLOGY） | 三态判定语义、状态流转（§0.3）、阈值缺省值 | 每次会话（已随 METHODOLOGY §0/§1 常驻） |
| L2 验证时 | §2 判定规则 + §3 与当前任务对应的检查项类 + §4 报告格式 | 执行验证时加载（由任务契约路由） |
| L3 用时查 | §1 接口（写新验证器时）、§5 对照表格式（构造对照表时） | 用到时检索 |

**设计说明**：验证的"核心语义"（pass/fail/need_human、状态流转、默认阈值）很短，已由 METHODOLOGY §0/§1 常驻承载；本文提供的是**执行细节**（检查项定义、接口、格式），因此由代码（tools/verifiers/）固化、按需查阅，不需要常驻上下文。

---

## 0. 目标

把 METHODOLOGY §2/§3/§5 的检查项实现为**可执行测试用例**，统一判定语义（pass / fail / need_human），使验证**可复现、可审计、可评分**。

---

## 1. 验证器接口（Verifier）

每个检查项 = 一个验证器（Verifier）。实现位于 `tools/verifiers/`。

### 1.1 结果结构 CheckResult

| 字段 | 类型 | 说明 |
|---|---|---|
| check_id | str | 检查项编号（如 NC-01） |
| status | pass / fail / need_human | 判定结果 |
| evidence | str | 一句证据/说明 |
| details | dict | 附加数据（如逐行误差表） |

### 1.2 验证器基类

```python
class Verifier(ABC):
    check_id: str        # 唯一编号
    name: str            # 可读名称
    def check(self, artifact: Path, ctx: dict) -> CheckResult: ...
```

- **只读**：验证器不修改产物，只读取并判定；
- **注册**：子类经 `register` 装饰器进入注册表，用 `check_id` 定位；
- **批量运行**：`run_verifiers(check_ids, artifact, ctx) -> list[CheckResult]`。

### 1.3 调用约定

- `artifact`：被验证产物的路径（推导结果、数值输出、对照表 JSON 等）；
- `ctx`：上下文 dict——`default_tol`（默认阈值）、`task_contract`（任务契约）、其他所需参数。

---

## 2. 判定规则

### 2.1 三态语义

- **pass**：有证据表明检查通过；
- **fail**：有证据表明检查不通过（必须给出原因，交还生成者）；
- **need_human**：自动化无法判定（缺参考值、需领域判断），转用户复核。

### 2.2 阈值约定

- 数值类默认相对误差阈值：`1e-3`（任务契约可覆盖）；
- 量级估计类：允许因子 2 以内偏差，超出须说明；
- 收敛阶类：要求呈现收敛趋势与阶数估计，不要求特定阶数。

### 2.3 状态流转

验证结果组合驱动 METHODOLOGY §0.3 状态机：

```
derived → 全部相关检查 pass → verified
        → 任一检查 fail     → rejected（附报告回退）
        → 任一 need_human   → 待用户复核后定
```

agent 无权自行把 need_human 当 pass。

---

## 3. 检查项清单（= 验证器目录）

> 实现位置：`tools/verifiers/checks/<编号>.py`。标注"（若适用）"的检查在任务契约声明适用时启用。

### 3.1 结构检查（符号推导）

| ID | 名称 | 判定标准 |
|---|---|---|
| SC-01 | 编译/语法通过 | 引擎可执行，无错误 |
| SC-02 | 数学结构自洽（自由指标平衡类） | 等式两边结构自由度一致（若适用，如张量指标平衡、矩阵维度一致、级数阶次一致） |
| SC-03 | 数学结构自洽（缩并类） | 无非法运算（若适用，如非法缩并、维度不匹配的乘积） |
| SC-04 | 对象类型/阶数 | 与定义一致（若适用，如张量类型/阶数、矩阵形状、函数参数类型） |
| SC-05 | 内禀性质 | 对称性/恒等式/行列式等（若适用） |
| SC-06 | 无未定义符号 | 全部符号在假设清单中定义 |

### 3.2 物理检查（符号推导）

| ID | 名称 | 判定标准 |
|---|---|---|
| PC-01 | 量纲/单位一致 | 单位制显式声明且两端一致 |
| PC-02 | 极限情形 | 参数取极限值退化为已知特解（需数值化支撑） |
| PC-03 | 规范/坐标无关性 | 规范不变量成立（若适用） |
| PC-04 | 文献/特例锚点 | 与已知结果一致（数值化比对） |

### 3.3 数值检查（数值计算/复现）

| ID | 名称 | 判定标准 |
|---|---|---|
| NC-01 | 锚点对照表 | 全部判据误差 ≤ 阈值（见 §5 格式） |
| NC-02 | 收敛阶检验 | 网格/步长/截断参数呈收敛趋势 |
| NC-03 | 守恒残差 | 守恒量残差在容差内（若适用） |
| NC-04 | 环境可复现 | 编译器/版本/节点/并行库已文档化 |
| NC-05 | 运行链完整 | 各步骤依赖明确，可断点续跑 |
| NC-06 | 外部锚点对照 | 至少一处与独立实现/文献数值/用户数据的对照（非同一代码重复运行） |

### 3.4 审查检查（验证审查）

| ID | 名称 | 判定标准 |
|---|---|---|
| RC-01 | 生成-审查解耦 | 审查者与生成者独立 |
| RC-02 | 可打分判定 | 逐项给出 pass/fail/need_human 与理由 |
| RC-03 | 分歧日志 | 与用户判断的分歧已记录（knowledge/review-lessons.md） |
| RC-04 | 交叉验证独立性 | 两条交叉路线实现层面独立（不同库/算法/来源），共享同一 bug 的交叉验证无效 |
| RC-05 | 测试设计审查 | 契约测试计划**明细层**（因素/通道映射）经怀疑派评估器审查：因素枚举完整 / 因素独立 / 判据可判定 / 方法盲区 / 锚点充分；审查结论（已覆盖因素/盲区清单/意见）回填**概要层**供研究者确认（见 METHODOLOGY §5.5） |

### 3.5 约定检查（跨任务类，若产物涉及约定）

| ID | 名称 | 判定标准 |
|---|---|---|
| CC-01 | 约定一致性 | 产物涉及约定类自由度（符号约定/单位制/坐标/归一化/相位等，见 METHODOLOGY §1.2 物理量定义）时：约定已显式声明，且关键恒等式按声明验证通过（判据：按声明的约定推导，恒等式两端一致；例：负阶/共轭关系、符号选择、归一化自洽） |

---

## 4. 验证报告格式

验证完成后产出报告（写入 run/）：

```markdown
# 验证报告：<产物名>

- 产物路径：...
- 任务契约：<id>（run/<id>.md）
- 验证时间 / 验证器版本：

| 检查项 | 名称 | 判定 | 证据 |
|---|---|---|---|
| NC-01 | 锚点对照表 | pass | 全部判据 ≤ 1e-3 |
| ... | ... | ... | ... |

## 结论
- 状态：verified / rejected / 待用户复核
- 剩余风险与说明：...
```

---

## 5. 锚点对照表格式（NC-01 输入）

JSON 文件，schema 见 `tools/verifiers/schemas/anchor_table.schema.json`：

```json
{
  "criteria": [
    {
      "name": "判据名（物理量，含单位）",
      "value": 1.234e-3,
      "reference": 1.235e-3,
      "tolerance": 0.001,
      "note": "来源：文献公式编号/页码"
    }
  ]
}
```

- `tolerance` 缺省时用 ctx 的 `default_tol`（默认 1e-3）；
- `reference` 缺失 → 该行 need_human（缺锚点，请求用户提供）。

---

## 6. 与后续 Step 的关系

- **Step 4A（推导 vertical slice）**：用 SC/PC 系列 + NC-01 验证真实推导产物；
- **Step 5（评测集）**：评测判据直接调用本协议的验证器（同一套判定，不另起炉灶）；
- 新检查项的加入流程：先写入 METHODOLOGY（规则层），再在 `tools/verifiers/` 实现（执行层），缺一不可；
- **执行顺序**：RC-05（测试设计审查）先于其他检查——测试计划不过审，不进入执行。
