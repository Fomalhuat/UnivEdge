# AGENTS.md — UnivEdge 智能体导航（地图）

本文件是 UnivEdge 的地图（渐进式披露）：只给方向，细节在各目录内。

## 定位

UnivEdge 是一个物理科研领域内核（domain kernel）——注入宿主运行时构成科研助手（agent）。核心信条：**做一步检查一步，从正确的锚点出发**。

- 人类用户掌舵：下达子任务、复核锚点、批准副作用。
- agent 执行子任务：定位上下文 → 选择工具 → 执行 → 验证 → 返回可审计结果 + 建议。
- LLM 永不手算作为最终产物：推导由符号引擎代算，数值由执行环境运行。

## 目录导览

- `docs/` — 设计档案与变更史（01 调研 / 02 需求 / 03 决策 / 04 结构审查 / 05 运行时契约与组件归属 / 07 内核复盘与改进方向 / 08 内核维护工作流）——**记录"为什么这样设计"，不随内核修订同步更新；运行规范以 METHODOLOGY.md / VERIFICATION.md 为准**
- `knowledge/` — 记录系统（agent 可读）：**内核预置**——约定注册表 conventions.md、复核教训 review-lessons.md（基层）；**领域资产**（非内核预置，使用者按需自建；GitHub 发布版不含，本地完整版保留）——基准结果库 benchmarks.md、主张注册表 claims.md、文献库 papers/、教训例子层 review-lessons-examples.md
- `skills/` — 可移植技能包（SKILL.md，project / custom / global 三作用域）
- `tools/` — 自研工具 + 执行环境适配层（本地沙箱 / 远程 HPC）
- `config/` — 环境无关配置（模型路由、权限策略、运行时契约 runtime-contract.md）
- `run/` — 产物存储（可插拔，宿主提供）：子任务审计产物（契约/验证报告/数据/脚本），可重建不入版本库；非本体内置目录（见 METHODOLOGY §1.6）

## 运行原则（摘要，完整规范见 METHODOLOGY.md；验证规则见 VERIFICATION.md）

1. **锚点原则**：未锚定 → 只能 claimed；锚点缺失 → 请求锚点，不给结论。
2. **检索教训**：每个子任务开始前，加载 review-lessons 基层（五个根因方向）内省风险——具体例子按需展开。
3. **工具代算 + 审查**：结构检查 → 物理检查 → 基准验证 → 人工复核。
4. **任务契约**：执行子任务前与用户确认"完成定义 + 验证标准 + 测试计划"。
5. **测试设计审查**：测试计划须经怀疑派评估器审查（因素枚举/独立性/判据可判定/方法盲区/锚点充分），研究者确认方向、AI 审查细节——审查标准本身也要被审查。
6. **权限门**：写文件、提交远程计算作业等副作用需用户批准。
7. **产物格式**：强制带 假设清单 + 有效域 + 误差预算。
8. **验证器**：每个检查项 = tools/verifiers/ 下的可执行验证器（pass/fail/need_human）。

## 工具注册表（持续扩充）

- 符号引擎：xAct/Mathematica、SymPy …（按任务路由）
- 执行环境：Python/C++/Fortran 沙箱、远程 HPC（SLURM）
- 外部：文献检索、MCP 插件

## 约定（正式版进 knowledge/）

符号约定、单位制、规范选择、坐标约定……（按领域填充，见 METHODOLOGY §1.2 物理量定义）
