# METHODOLOGY.md — UnivEdge 方法论规格

> 版本：v0.9（领域内核版）｜ 日期：2026-08-29
> 本文是 **UnivEdge 领域内核**的核心文件：定义"一个懂物理科研的 agent 如何行动"。
> 作为运行规范随仓库分发，供任意宿主环境使用——因此要求**自包含、自解释、不依赖具体使用者语境**。
> 与部署形态解耦（docs/06）：只谈"做什么、怎么验证"，不谈宿主。
> 配套：docs/（设计依据）、config/runtime-contract.md（运行时要求）。
> v0.4 变更（2026-08-13/14/15）：任务契约增加"测试计划"段（测试设计双层审查，
> 概要层给研究者 / 明细层给评估器，审查结论回传概要层）；增加"已知参照值"软性字段；
> 物理量定义标注建议性；§1.1 增加"检索教训"步骤（加载 review-lessons 基层，每次必做）；
> §1.5 固化沉淀元思想（错误类型只保留线性独立的根因基，不固化具体例子）；
> §1.6 新增产物存储定义（可插拔，宿主提供——run/ 仅为当前宿主实现，非本体内置目录）；
> 2026-08-16 修订：移除原 §8（与后续 Step 的关系，冗余——信息在 README 路线/VERIFICATION §6）与原 §9（内核维护工作流，非执行规则——迁移至 docs/08）。
> v0.5 变更（2026-08-24，使用者视角评审，rp=12 复盘）：§1.2 契约新增"启动自省"必答字段
> （B1-B4 + P 逐条内省）与"配置基线"必填字段（数值/复现类）；§3.3 澄清"最小改动 = 最小变更面
> 非最小复制面"。根因：协议被写为"建议"而非"必答字段"，导致无协议裸奔（rp=12 任务 5 错误全漏）。
> v0.6 变更（2026-08-24，内核守护者元评审）：§5.1 怀疑派评估器改为"经运行时契约 R7 实例化
> 独立评估器"（不再同 agent 切换角色），新增"审查输入包"自包含定义 + 三级解耦降级（L1/L2/L3）
> 诚实声明；配套 runtime-contract 新增 R7、VERIFICATION RC-01 改分级声明 + 检查项加实现形态列。
> v0.7 变更（2026-08-25，执行 agent 使用反馈，4.1 验证闭环第一个数据点）：§1.2 契约新增
> "副作用前核对清单"字段（提交 HPC 作业 / 跨实现对比前逐项勾选，未勾选不得提交）。根因：
> 软强制被证在高强度任务下"被跳过"（契约/自省缺席、R7 触发 0 次），契约从叙事文档补充
> 提交前 checklist，把门控前置到副作用点。
> v0.8 变更（2026-08-27，回到外部参照 OpenAI/Anthropic harness-engineering 的战略反思）：
> 副作用前核对清单下沉为可执行代码——`tools/hpc/slurm_submit_template.sh` CHECK 字段自校验，
> 未填全则拒绝提交。根因：约束必须"可强制执行"，文档只是地图；此为本内核"可执行化"的第一个切片。
> v0.9 变更（2026-08-29，R7 审查揭示，run/review/r7-e5306b47）：§1.6 宿主字样修订——
> "WorkBuddy 宿主下"→"宿主提供"，与 dsh 为主宿主的实际实践及 v0.4 变更记录对齐。

---

## 使用方式（加载协议）

本文遵循**渐进披露**，**不需要每次完整读取**。按以下分层加载：

| 层级 | 内容 | 加载时机 |
|---|---|---|
| L0 常驻 | `AGENTS.md`（地图） | 每次会话 |
| L1 核心 | 本文 §0 总则 + §1 通用框架；`knowledge/review-lessons.md` **基层**（五根因方向） | 每次会话（约 120 行） |
| L2 任务 | §2–§6 中与当前任务类型对应的一节 | 由任务契约（§1.2）路由，按需加载；验证时配 `VERIFICATION.md` 对应章节 |
| L3 引用 | §7 附录、review-lessons 例子层、docs/、knowledge/、config/ | 用到时检索 |

**写作约定（维护本文时必须遵守）**：

- 每节**自包含**：节内自解释，不依赖其他节的内容才能理解；
- 关键规则放节首；用短句、高指令密度，避免散文；
- 新增章节前先评估"是否必须进本文"——可放 docs/ 或 knowledge/ 的，不进；
- 任何修改不得破坏上述分层（核心段保持精简，任务段可独立加载）；
- **元规则（本文的终态约束）**：本文固化的应是**线性独立的根因基**，不是具体例子。
  新增规则前先问：这是否是已有规则的具体实例？若是 → 进 knowledge/ 例子层；
  只有无法由现有基线性组合的全新根因，才允许进本文。
  **本文的结构性修改应收敛**——"完成"的定义即不再需要新增机制，仅 knowledge/ 持续增长。

---

## 0. 总则

### 0.1 目标

把优秀物理研究者的行为模式固化为可执行规范，使 agent 在以下五类任务上产出**可审计、可复核、可复现**的结果：

1. 符号推导；
2. 数值计算与文献复现；
3. 文献核对；
4. 验证审查；
5. 工具与技能开发。

### 0.2 最高原则：锚点原则

> 做一步检查一步，从正确的锚点出发。

- 任何产物必须锚定到**独立可验证的参照**：已知极限、守恒律、文献基准、已复核的上游结果；
- **未锚定的结果只能标记 claimed，不得进入结论**；
- 锚点缺失时，agent 的义务是**请求锚点**（向用户或文献），而不是给出结论。

### 0.3 产物状态机

agent 无权自行升级状态（除 claimed→derived 外，均需验证器或用户确认）：

```
claimed → derived → analytically-checked → numerically-checked → data-supported
                                  ↘ rejected（任何检查不通过）
```

### 0.4 产物通用格式

每个产物必须携带：

- **假设清单**：建模/推导中所有理想化与假设（含代价说明；所有物理量含义在此定义或引用）；
- **有效域**：参数范围与适用条件；
- **误差预算**：误差来源与量级估计；
- **状态标签**：§0.3 中的状态；
- **锚点引用**：锚定了什么、参照在哪。

---

## 1. 通用执行框架（所有任务类型）

### 1.1 子任务服务循环

每次执行一个子任务，按序：

1. **定位上下文**：当前子任务属于物理方法循环（提出/建模/推导/计算/检验/结论）的哪个环节？与 knowledge/ 已有约定教训、产物存储（宿主提供，见 §1.6）已有产物的关系？
2. **检索教训（每次必做）**：加载 `knowledge/review-lessons.md` 的**基层**（五个根因方向，~25 行，常驻级成本）——用它内省当前任务的潜在风险；涉及某基时按需展开对应例子（见 `knowledge/review-lessons-examples.md`——**领域资产，使用者自建；未建则跳过展开，仅用基层内省**）。**基层是根因方向清单，不是具体教训清单。**
3. **选工具**：从工具注册表按任务类型路由（符号推导→符号引擎；数值→执行环境；文献→检索/解析）；
4. **执行**：工具代算——agent 负责规划与解释，不手算最终产物；
5. **验证**：按任务类型对应的验证协议（§2–§5）执行；
6. **返回**：可审计结果（含验证报告）+ 下一步建议。

### 1.2 任务契约（执行前必做）

子任务执行前，agent 产出任务契约并与用户确认后才执行：

```
任务契约模板：
- 启动自省（产出契约前必答，对应 review-lessons 基层 B1-B4 + P，见 §1.1 第 2 步）：
  【逐条回答】① 是否已加载 review-lessons 基层并逐条内省（未加载先加载）？
  ② B1 无依据断言风险？——未锚定的结论只能 claimed；
  ③ B2 语义自由度（符号/单位/坐标/规范/截断阶/归一化/相位）是否显式声明？
  ④ B3 工具是否在有效域内、库默认约定是否与标准一致？
  ⑤ B4 测试设计是否有盲区（测过≠测对）？
  ⑥ P 是否跳过前置流程——配置基线是否已建立、协议是否已加载、生产链是否已完整复刻？
- 目标：一句话说明要得到什么
- 物理量定义（建议性）：公式级定义。判据：**凡影响该量数值解读的自由度，均须显式声明**——
  设想"两位有经验的研究者各自按此定义计算，是否会得到同一数值"，若可能分歧则该自由度必须声明。
  常见自由度（不限于）：截断阶数 / 展开参数 / 符号约定（相位、符号、索引顺序）/
  单位制 / 坐标与规范 / 归一化 / 定义域。
  论文语境已明确（标准记号）时可写"语境已明确"并省略；语境不明确时建议公式级描述。
- 已知参照值（若有，软性）：研究者若已知收敛值/文献值/锚点数值，可在此给出。
  agent 在锚点缺失时优先请求（"你知道参照值是多少吗？"），而非自行假设。
- 配置基线（数值计算/文献复现类任务必填）：外部库/生产链的精确配置逐项列出——
  表生成 / 演化 / 提取 / 拟合 / 网格 / 时间 T 每步参数，并附配置来源文档清单
  （README / final_results / verification_summary 等）。**未建立基线前禁止修改参数或提交作业。**
  反例：rp=12 任务跳过生产配置文档 → 8-10h 无效诊断（流程类根因 P1）。
- 副作用前核对清单（提交 HPC 作业 / 跨实现对比前必填，逐项勾选，未勾选完成不得提交）：
  步长 h / 截断 lmax / 表范围 / GL 节点 / r_p / reference（对照值）/ 对端配置（对照实现是否用同一套参数）。
  反例：频域全量 h=0.02 而对照端 h=0.005——"对端配置"一栏空缺，被用户拦截才避免 ~150min 返工（根因 B1+P）。
  执行：使用 `tools/hpc/slurm_submit_template.sh`——其 CHECK 字段自校验，未填全则脚本拒绝提交（exit 1），
  把核对从"人工自觉"变成"脚本可执行的前置条件"。
- 完成定义（DoD）：什么算完成（可判定的标准）
- 验证标准：用什么检查/对照来证明正确（含约定检查、外部锚点，见 VERIFICATION）
- 测试计划（强制，P0）：分两层——
    【概要层 · 研究者看】测试项列表：测试名 + 判据（可判定）+ 目的（一句话）；
      附怀疑派评估器审查结论（已覆盖因素 / 盲区清单 / 审查意见，见 §5.5）。
    【明细层 · 评估器审查用，研究者按需查阅】因素/通道映射明细
      （附契约附录或产物存储独立文件，渐进披露 L3 层）。
  测试设计须经怀疑派评估器审查（§5.5）——研究者确认方向，AI 审查细节。
- 锚点：独立参照（已知结果/文献/守恒律）
- 权限等级：L0 / L1 / L2（见 1.3）
- 预计产物：文件名 + 格式
```

契约本身写入**产物存储**（§1.6），作为该子任务的审计起点。

### 1.3 权限门

- **L0 只读**：查文件、检索、推导预览——自动放行；
- **L1 本地写**：写产物存储——默认需确认（可配置批量放行）；
- **L2 高危**：提交远程计算作业、网络访问、改删外部文件——必须逐次批准；
  **提交远程计算作业时，须附已勾选的副作用前核对清单（§1.2）随提交请求一并呈现，供批准核对**。
  核对清单由 `tools/hpc/slurm_submit_template.sh` 的 CHECK 字段自校验——未填全则脚本拒绝提交，门控下沉到可执行代码。

### 1.4 防死循环（风暴抑制）

- 同一工具/同一步骤连续失败达阈值（默认 3 次）→ **强制反思**：停止，分析根因，输出"失败原因 + 修正方案"后再继续；
- 总迭代上限 MAX_ITER（默认 8）；超限 → 停止并报告，不硬闯。

### 1.5 复核回写

- 用户的每次复核意见必须**沉淀为规则**（进 linter/检查项/文档），而不是只改当次结果；
- 沉淀入口：`knowledge/review-lessons.md` 或对应检查清单；
- **沉淀原则（元思想）**：固化的是从具体例子中抽象出的**线性独立的根因基**，不是例子本身——
  像高明的人类学习者：不背错题，而从错误提炼根本原因，从而避免一类错误。
  具体教训是无限集，根因基是有限集——错误类型构成一个集合，只保留其**线性独立的一组基**
  （当前基：B1 无依据断言 / B2 语义歧义 / B3 工具方法误用 / B4 验证失效·拦截器）。
  新教训先归入已有基；只有出现无法由已有基线性组合的全新根因才建新基。

### 1.6 产物存储（可插拔，宿主提供）

- **定义**：子任务审计产物的存储位置（契约 / 验证报告 / 数据 / 脚本），由**宿主提供**，不是本体内置目录；
- **接口**：宿主须提供可写入/读取/列举的产物存储；本体内只定义产物格式（契约模板 §1.2、报告结构、数据规范），不绑定存储实现；
- **当前实现**：宿主提供 `run/`（每个子任务一个子目录，如 run/step4a/；当前 dsh 宿主会话产物即落于此）；
  其他宿主可为 `~/.univedge/runs/`、项目目录、云存储等——**换宿主时 run/ 不迁移，产物格式仍兼容**；
- **可拔性**：本内核不依赖任何特定存储路径；缺失产物存储时，产物仍可在会话内交付（宿主负责持久化）；
- **不入版本库**：产物存储默认排除在仓库版本控制之外（当前实现 .gitignore 排除 run/），因其可重建。

---

## 2. 任务类一：符号推导

> 适用时机：任务契约为"符号推导"类型时加载本节。

### 2.1 铁律

**agent 永不手算作为最终产物。** 推导由符号计算引擎（xAct/Mathematica、SymPy 等，按任务路由）执行。

### 2.2 流程（符号推导可靠性管线）

```
起草方案 → 引擎执行 → 结构检查 → 物理检查 → 基准数值验证 → 人工复核 → 产出
   ▲                                                              │
   └──────── 任一环节不通过 → 携检查报告回退到起草（≤ MAX_ITER）─────┘
```

### 2.3 结构检查项（机械可查）

- 编译/语法通过；
- **数学结构自洽**（若适用，按领域约定）：如张量型推导的指标平衡/缩并/阶数一致、矩阵运算维度一致、级数展开的阶次一致等；
- 对称/反对称性、该领域已知的结构恒等式、内禀性质（若适用）；
- 表达式中不含未定义符号（所有符号须在假设清单中定义）。

### 2.4 物理检查项

- **量纲/单位一致**：若采用约定单位制（如自然单位制 G=c=1，其中 G 为引力常数、c 为光速），须在假设清单中显式声明；
- **极限情形**：令关键物理参数（其含义在假设清单中定义）取极限值——趋于 0、趋于无穷、或使对称性增强的特殊值——结果应平滑退化为已知的简单情形或已知特解；
- **规范/坐标无关性检验**（若适用，如规范不变量）；
- **结果与已知特例/文献公式一致**（锚点）。

### 2.5 验收标准

通过 2.3 + 2.4 + 基准数值验证（特殊情形数值化 vs 锚点），且用户复核通过，方可标 verified。

---

## 3. 任务类二：数值计算与文献复现

> 适用时机：任务契约为"数值计算/文献复现"类型时加载本节。

### 3.1 原则

**可复现性优先**：环境、参数、运行链全部文档化。

### 3.2 流程

读方法（论文/既有代码）→ 重实现或接线 → 运行 → 锚点对照 → 收敛与误差报告 → 人工复核

### 3.3 验证协议

1. **锚点对照表**：判据 / 复现值 / 参考值 / 误差（固定模板，见附录 A）；
2. **误差通道分解**：误差按物理来源/通道分而治之，禁止盲调参数；
3. **最小改动对照实验**：一次只改一个因素，证明因果。**"最小改动"= 最小变更面**（完整复刻已验证的生产链，只改目标变量），**不是最小复制面**（挑最简化的实现改参数）。先建立生产链基线（见 §1.2 配置基线），再单因素变动。反例：rp=12 任务把"只改 rp"误读为"选窄网格简化文件改参数"，而非"完整复刻宽网格生产链只改 rp"（流程类根因 P2）。
4. **收敛阶检验**：覆盖管线每一环节（数据生成、演化、后处理），不遗漏；
5. **环境可复现**：编译器/解释器、节点、并行库、版本写入脚本；
6. **运行链文档化**：各步骤串行依赖明确，可断点续跑。

### 3.4 验收标准

锚点对照表全部判据误差在约定阈值内（或偏差来源已解释）+ 收敛报告完整 + 用户复核通过。

### 3.5 新代码实现任务的分解模板（最小可验证单元先行）

> 既有"阶段门控评审"（docs/08 §4）在数值实现上的**落实强化**——不新增机制，只规定子任务划分。
> 实例：rewrite3d 一次性实现 ~500 行后统一验证，错误方程（缺几何项）跑 1.5 天才暴露；
> 若先做 1D 降维验证（vs 已验证参考/已知场），1 小时内暴露。

- **新代码实现任务强制分解**为可验证子任务，依次门控：
  A. **方程核对**（PC-05）：代码内嵌 PDE 从已验证参考实现工具代算转换/文献交叉验证；
  B. **降维验证**（成本最低先行）：1D/2D 简化 vs 已验证参考或已知场/剖面，收敛对照；
  C. **组件验证**：源注入 / 提取管线 / 边界条件逐项对照；
  D. **全量演化**：目标配置 + 全通道验证。
- 每个子任务产出可验证交付物，完成即触发**轻量阶段评审**（claimed → verified 升级，成为下一阶段锚点）；
- 禁止"一次性实现全量再统一验证"。

---

## 4. 任务类三：文献核对

> 适用时机：任务契约为"文献核对"类型时加载本节。

### 4.1 流程

抽取目标（公式/结论/约定）→ 溯源（论文标识/原文页码/公式编号）→ 约定对照（符号约定、单位制、规范/坐标选择、记号）→ 一致性检查（与 knowledge/ 约定注册表比对）→ 产出核对报告

### 4.2 验收标准

- 每个引用带精确溯源；
- 约定差异**显式列出**（不默认一致）；
- 涉及推导的引用交叉执行 2.4 物理检查。

---

## 5. 任务类四：验证审查（怀疑派评估器）

> 适用时机：需要独立审查/复核他人产物（或本系统产物）时加载本节。

### 5.1 角色设定与解耦等级

- 与生成者**解耦**（独立角色、独立规范），只审查不生成；
- 默认立场：怀疑——"合格 = 找不出实质问题"，而非"看起来没问题"。
- **实例化（经运行时契约 R7）**：由宿主实例化一个**独立上下文的评估器**，经文件通信审查；
  禁止在同一 agent 内切换角色自我审查。
- **审查输入包**（生成者交给评估器的输入，自包含）：
  - 包含：① 产物——待审查的产出（报告/数据/脚本/契约），附生成者声称的状态（claimed/derived）；
    ② 锚点——独立参照及其来源（已知值/文献基准/守恒律/用户提供的真值，见 §0.2 锚点原则）；
    ③ 审查清单——要检查的项，即 VERIFICATION 检查项 ID 列表（如 NC-01/RC-05/CC-01）；
    ④ 假设与声明——产物的假设清单、有效域、误差预算（见 §0.4 产物通用格式）。
  - 不包含：生成者的推理过程、中间尝试、失败假说与排除过程——避免评估器被生成者的思维链锚定。
- **三级解耦降级（诚实声明）**：实际解耦等级由宿主能力决定，验证报告必须标注：

  | 等级 | 定义 | 达成条件 |
  |---|---|---|
  | L3 模型解耦 | 独立上下文 + 独立模型 + 独立角色 | 宿主满足 R7 且支持按角色路由不同模型 |
  | L2 上下文解耦 | 独立上下文 + 独立角色，同模型（含同模型的不同推理档位） | 宿主满足 R7（无跨模型路由） |
  | L1 伪解耦 | 单 agent 切换角色 | 宿主不支持 R7 |

  宿主不支持 R7 时审查降级 L1，须显式声明"伪解耦、效力打折"；禁止虚标解耦等级。

### 5.2 评分标准（可打分，不拍脑袋）

- 数值/复现类：用锚点对照表逐项判定（通过 / 不通过 / 需人工），给出偏差解释；
- 推导类：按 2.3 / 2.4 检查项逐项判定；
- 主观项（如有）：用显式标准判定，且措辞刻意设计（用词会塑造产出）。

### 5.3 调优机制

- 记录评估器判断与用户判断的**分歧日志**（`knowledge/review-lessons.md`）；
- 按分歧迭代评估器规范——怀疑派立场不是天生的，需要按分歧记录主动调优。

### 5.4 边界

评估器不得"说服自己问题不重要"；发现问题 → 明确标不通过并给出理由，交还生成者。

### 5.5 测试设计审查（审查标准本身要被审查）

> 原理：产物可能自洽而不正确——因为**审查标准/测试设计本身可能有盲区**。
> 典型形态：测试者以为覆盖了某因素，实际漏测了与之耦合的另一因素
> （实例见 docs/07 复盘 B2）。

- **审查对象**：任务契约中的"测试计划"（§1.2）——测试设计本身，而非产物；
- **审查时机**：契约阶段（执行前）——研究者确认方向，AI 审查细节；
- **双层审查原则**：研究者不必掌握测试方法全部细节，AI 怀疑派评估器承担细节审查；
- **输入分层**：评估器审查**明细层**（因素/通道映射明细，契约附录或产物存储独立文件）；
  研究者只读**概要层**（测试项 + 判据 + 目的）——不被细节淹没；
- **审查项（对应 VERIFICATION RC-05）**：
  1. **因素枚举完整性**：测试覆盖所有相关自由度？未覆盖因素显式列为盲区；
  2. **因素独立性**：最小改动对照中一次只改一个因素（耦合因素须显式拆分，否则可能"以为在测 A 实际同时动了 B"）；
  3. **判据可判定性**：每个测试的 pass/fail 标准可判定（数值/区间），非抽象描述；
  4. **方法盲区**：测试方法本身是否可能给出误导性结论（如收敛判据只测部分通道）；
  5. **锚点充分性**：测试是否锚定到独立参照（外部锚点，非同一代码重复运行）；
- **输出**：测试设计审查意见（通过 / 需修订 / 需人工），附理由，交生成者修订后复审；
- **结论回传**：审查结论（已覆盖因素 / 盲区清单 / 审查意见）回填契约概要层，供研究者确认方向——
  研究者无需读明细，也能获得"测试设计是否可靠"的独立判断，并可基于领域知识补充盲区；
- **不通过时**：不得进入执行阶段。

---

## 6. 任务类五：工具与技能开发

> 适用时机：任务缺少可用工具/技能时加载本节。

### 6.1 触发

任务无可用工具/技能，或现有工具频繁出错时。

### 6.2 流程

识别缺口 → 开发工具（脚本/包装）或提炼技能（SKILL.md：问题特征 → 工具 → 步骤 → 验证）→ 自验证（最小用例跑通）→ 注册表登记（名称 / 适用任务 / 使用方式 / 验证记录）

### 6.3 验收标准

注册前必须通过最小验证；登记信息完整；与既有工具不重复。

---

## 7. 附录：模板与清单

### A. 锚点对照表模板

| 判据 | 复现值 | 参考值 | 误差 | 判定 | 备注 |
|---|---|---|---|---|---|
| ... | ... | ... | ... | 通过/不通过/需人工 | 偏差解释 |

### B. 任务契约模板

见 §1.2。

### C. 推导检查清单（通用）

```
[结构] 编译 / 数学结构自洽（若适用：指标平衡、缩并、阶数一致）/ 符号均已定义
[物理] 量纲 / 极限情形 / 规范坐标无关性（若适用）/ 文献锚点
```

### D. 数值检查清单

```
[收敛] 网格 / 步长 / 截断参数 的收敛阶
[守恒] 守恒量残差（若适用）
[对照] 锚点对照表
[复现] 环境与运行链文档
```
