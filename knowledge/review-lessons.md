# 复核教训库（Review Lessons）

> 记录系统：把踩坑沉淀为**线性独立的根因基**，而非无限增长的具体清单。
> 设计原则（元思想，本库的最高原则）：**领域内核固化的是从具体例子中抽象出的规律，不是例子本身**——
> 像高明的人类学习者：不背错题，而从错误提炼根本原因，从而避免一类错误。
> 具体教训是无限集，根因基是有限集——错误类型构成一个集合，内核只保留其**线性独立的一组基**。
> AI 每次任务开始时加载【基层】（5 个根因方向，短），具体例子【按需展开】。
> 关联：METHODOLOGY §1.1（触发规则）、§1.5（复核回写）、§5.5（测试设计审查）、VERIFICATION RC-03。

---

## 加载方式

```
L1 常驻（每次任务开始）→ 加载【基层】= 下面 5 个根因方向（约 25 行）
L3 按需展开 → 涉及某基时，读 review-lessons-examples.md 该基下的具体例子与验证方法（领域资产，使用者自建；未建则跳过）
```

> 例子层（具体教训）独立于本文件，见 `review-lessons-examples.md`——它是**领域资产**，
> 由使用者自建并随使用持续增长，默认不随"给别人用"的发布版打包。本文件只保留基层（有限集）+ 流程 + 分歧日志。

---

## 基层：五个线性独立的根因方向（AI 的任务内省清单）

**B1 无依据断言**：我是否在产出缺乏真实依据的结论（捏造公式/数值/引用）？——未锚定的结论只能 claimed；LLM 永不手算作为最终产物。
**B2 语义歧义**：影响数值解读的自由度（符号约定、单位制、坐标/规范、截断阶数、归一化、相位）是否显式声明并对齐？——若两个研究者按此定义会算出不同数值，则该自由度未声明。
**B3 工具/方法误用**：当前工具/算法是否在其有效域内运行？库的默认约定是否与标准一致（转置、相位、索引）？——边界处显式测试；约定不确定时数值交叉验证为权威，解析只做锚点。
**B4 验证失效（meta 层）**：验证/测试设计本身是否可能有盲区，导致漏过 B1–B3？——"测过了"不等于"测对了"；因素拆全、判据可判定、锚定独立参照。
**P 流程执行（gate 前置，与 B1-B4 正交）**：我是否**跳过必须的前置动作就直接动手**（未建立配置基线就改参数、未加载协议就执行、未完整复刻生产链就"最小改动"、未读生产配置文档就提交作业）？——流程类错误**教训库救不了，只有门控能拦**：副作用前 / 结论前 / 交付前三处强制自省（见 §1.2 契约"启动自省"）。

> 说明：B4 是拦截器（作用在 B1–B3 之上）；P 是**流程执行维**（B1-B4 是"怎么做错"的知识判断，
> P 是"根本没按流程做"的执行缺失）——两者正交，故并列。过程迷失是果非因，不单列。

---

## 新增教训流程

1. **先归类**：判断属于哪个基（B1–B4 + P）→ 加入 `review-lessons-examples.md` 该基例子区（领域资产，使用者自建；未建则先创建该文件）；
2. **无合适基才建新基**：基的数量应缓慢增长（基层必须保持短小）。基层数量**硬上限 6**——达到上限后，新基只能通过"归约/合并已有基"腾出位置，不得直接新增；
3. **建新基前先通过独立性检验（三重硬判据，缺一不可）**：
   ① **落点不同**：新基的"拦截动作"（要做什么来防这类错）与已有基的拦截动作在**类型**上不同（知识提醒 / 定义检查 / 工具检查 / meta 审查 / 流程门控）；
   ② **不可归约**：新基描述的错误不能被"已有基的组合"解释（反例："过程迷失"= 其他基未被拦截的后果，是果非因，不建）；
   ③ **双向反例**：必须给出至少一个"只属于新基、不属于任何已有基"的**真实错误实例**（否则新基是已有基的子集/组合）；且新基不能解释任何已有基的典型实例（否则新基覆盖了已有基）。
   三者齐备才允许建新基——错误类型集合只保留**线性独立的一组基**。
4. **同步沉淀**：每次新教训应关联到内核规则/检查项（METHODOLOGY / VERIFICATION / conventions），并回写对应文件。

## 分歧日志（评估器 vs 用户判断）

| 日期 | 评估器判断 | 用户判断 | 结论 | 沉淀 |
|---|---|---|---|---|
| 2026-08-13 | 缺口 A（物理量定义）最严重，应强制 | 研究者有责任说明情况，A 应降级 | 用户正确——任务理解偏差 ≠ 内核缺陷 | METHODOLOGY §1.2 |
| 2026-08-13 | 缺口 C（近似自省）应强制检查 | 忠实报告收敛即可，研究者会提要求 | 用户正确——C 降级为报告附注 | docs/07 |
| 2026-08-13 | 测试计划细节应全部给研究者 | 研究者未必了解测试方法细节，应双层审查 | 用户正确——概要给研究者、明细给评估器、结论回传 | METHODOLOGY §1.2/§5.5 |
| 2026-08-14 | 教训应逐条记载（具体清单） | 具体教训是无限集，应按类型记载、AI 只需思考方向 | 用户正确——类型层有限稳定，例子层按需展开 | 本文结构 |
| 2026-08-14 | 六类错误类型均保留 | 错误类型应找**线性独立的一组基**；过程迷失是果非因（由其他错误累积而成），不独立应剔除 | 用户正确——4 基替代 6 类；过程迷失并入 B4 拦截失效的说明 | 本文基层 B1–B4；METHODOLOGY §1.5 |
